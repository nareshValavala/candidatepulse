{
  "name": "candidatepulse-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CandidatePulse — real-time interview integrity-signal app (Express + SQLite + Claude). A signal, not a verdict.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "eval": "node evals/run-evals.mjs",
    "simulate": "node evals/simulate.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "better-sqlite3": "^12.10.0",
    "express": "^5.2.1"
  }
}
